name: build-windows

on:
  push:
    tags: ["v*"]
  workflow_dispatch:

jobs:
  windows-installer:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - uses: dtolnay/rust-toolchain@stable

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      # --- sidecar 1: findy-engine (graphify wrapper, PyInstaller onefile)
      - name: Build findy-engine sidecar
        working-directory: engine
        run: |
          pip install graphifyy pypdf pyinstaller
          pyinstaller engine.spec
        shell: bash

      # --- sidecar 2: rtk (Rust Token Killer)
      - name: Build rtk sidecar
        run: cargo install --git https://github.com/rtk-ai/rtk --locked
        shell: bash

      # Tauri expects sidecars suffixed with the target triple
      - name: Stage sidecars
        run: |
          TRIPLE=x86_64-pc-windows-msvc
          mkdir -p src-tauri/binaries
          cp engine/dist/findy-engine.exe "src-tauri/binaries/findy-engine-$TRIPLE.exe"
          cp "$USERPROFILE/.cargo/bin/rtk.exe" "src-tauri/binaries/rtk-$TRIPLE.exe"
        shell: bash

      - name: Install frontend deps
        run: npm ci

      - name: Build Tauri app (NSIS installer)
        run: npm run tauri build -- --bundles nsis
        env:
          TAURI_SIGNING_PRIVATE_KEY: ""

      - uses: actions/upload-artifact@v4
        with:
          name: findy-windows-installer
          path: src-tauri/target/release/bundle/nsis/*.exe
